# Industry engine — Python deps (all already present in the local env).
pandas>=2.0
numpy>=1.24
scipy>=1.11
statsmodels>=0.14
yfinance>=0.2.40
# stdlib used: sqlite3, urllib (PostgREST), json, subprocess
