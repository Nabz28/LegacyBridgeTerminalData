Industry-engine autonomous tick. Read industry-engine/RUN.md and industry-engine/state/progress.json, then execute EXACTLY ONE bounded step per RUN.md and STOP. Default step (if any basket is pending): run `python industry-engine/engine/controller.py --n 2` (it runs + grades + persists + commits). If no baskets are pending but some are needs_review/partial-and-improvable, do ONE basket's enrichment per RUN.md (edit engine/mapping.py, re-run `--only "<Sub Sector>"`, commit). If all baskets are resolved and the UI is wired (current state), do ONE CRITIQUE & HARDENING item from RUN.md instead — be a skeptic: audit a perfected basket for spurious/endogenous drivers, fix a theory-sign error, re-attempt a partial with better data, harden methodology, or polish + re-verify the UI (via the harness). Keep token use minimal; never run more than ~1 minute of work in a fire. Everything is checkpointed + committed, so if you are near a token/rate limit just verify state is consistent and stop — the next fire resumes automatically after any reset. Do not start if another tick appears to be mid-run.
